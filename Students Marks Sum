int marks_summation(int* marks, int number_of_students, char gender){
    int suma=0,i;
    for(i=0;i<number_of_students;i++){
         if((gender=='b') && (i%2==0 | i==0)){
             suma+=marks[i];
         }
         if((gender=='g') && (i%2!=0) && i!=0){
             suma+=marks[i];
         }
    }
    return suma;
}
