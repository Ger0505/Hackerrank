#include <stdio.h>
#include <math.h>

int main() 
{
    int n,i,suma=0;
    do{
        scanf("%i",&n);
    }while(n<10000 | n>99999);
    for(i=0;i<5;i++){
        suma+=(n%10);
        n=trunc(n/10);
    }
    printf("%i",suma);
    return 0;
}

